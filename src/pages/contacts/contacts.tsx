import React from "react";
import "./contacts.css";

export const Contacts: React.FunctionComponent = (props) => (
  <div className="contacts">
    <div className="first-main-page">Контакты</div>
  </div>
);
